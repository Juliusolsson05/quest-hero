# Secrets — never commit an API key. TrueForge keeps its own keys in SQLite
# outside the repo; anything here is for local tooling only.
.env
.env.*
!.env.example

# TrueForge session-id cache (NPC memory survives hub restarts; machine-local)
hub/.trueforge-sessions.json
*.key
secrets/

# Node
node_modules/
dist/
build/
.vite/
*.tsbuildinfo

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log

# TrueForge local state (SQLite lives in ~/Library/Application Support/trueforge)
.trueforge/
