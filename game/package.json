{
  "name": "quest-hero-game",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && vercel link --yes --project sf-quest --scope gustifinks-projects && vercel deploy --prod --yes"
  },
  "dependencies": {
    "playroomkit": "^0.0.97",
    "three": "^0.181.0"
  },
  "devDependencies": {
    "@types/three": "^0.181.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.5"
  }
}
