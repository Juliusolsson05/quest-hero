{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix game ci --include=dev && npm --prefix game run build",
  "outputDirectory": "game/dist",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60
    }
  }
}
